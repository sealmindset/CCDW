# Handoff History

## Archived 2026-07-30 12:39

# Handoff -- CCDW (Claude Code Docker Workshop)
_Written 2026-07-29 11:15 by /clear-it. Read this file in full before continuing work._

## 1. Goal
Make the CCDW terminal feel like a real macOS terminal — seamless copy/paste with no
web-layer barrier. Specifically: user can select text and copy it exactly like
Terminal.app (drag-select + ⌘C, auto-copy on release, right-click Copy/Paste, ⌘V paste).
Acceptance: native-quality selection/clipboard in the macOS app. **DONE this session.**

## 2. Current State
- **SHIPPED, committed, pushed to both remotes, built, deployed, running.**
- Branch `main`, HEAD `1dd8e26`. Working tree CLEAN. Pushed to `origin`
  (SleepNumberInc/CCDW) and `seal` (sealmindset/CCDW).
- The macOS terminal app was rewritten from a WKWebView-wraps-ttyd shell into a **real
  native terminal emulator (SwiftTerm)** that drives a `docker exec -it` PTY straight into
  the `claude-code` container. No ttyd / xterm.js / WKWebView / OSC-52 in the app path.
- App built to `~/Applications/CCDW Terminal.app` (SwiftPM `swift build -c release`, ad-hoc
  signed). Verified LIVE: app process up, spawned the expected PTY child
  (`docker exec -it -u coder -w /home/coder/Documents claude-code bash --init-file
  /opt/claude-code-docker/scripts/shell-init.sh`), no crash.
- Container `claude-code` up + healthy (unchanged this session). Browser terminal on
  :7681/:7682 (ttyd + tmux) deliberately LEFT AS-IS — only the native app changed.
- **NOT visually tested:** actual copy/paste *feel* (drag-select, ⌘C, right-click, ⌘V)
  needs Rob's hands — cannot GUI-test headless. Code paths match SwiftTerm's verified API.

## 3. Active Files
- `scripts/mac-app/Sources/CCDWTerminal/main.swift` -- NEW native app. SwiftTerm
  `LocalProcessTerminalView` subclass; docker discovery, `docker exec` PTY, all 4 copy
  behaviors, right-click menu, zoom, reconnect, container-down banner.
- `scripts/mac-app/Package.swift` -- NEW SwiftPM manifest; SwiftTerm pinned `exact("1.5.0")`.
- `scripts/mac-app/Package.resolved` -- NEW, committed for reproducible builds.
- `scripts/mac-app/build-terminal-app.sh` -- rewritten: `swift build -c release` → assemble
  + ad-hoc sign the `.app` bundle around the built binary.
- `scripts/mac-app/CCDWTerminal.swift` -- DELETED (old WKWebView wrapper).
- `.gitignore` -- added `scripts/mac-app/.build/`.
- `CHANGELOG.md` -- `### Changed` entry describing the rewrite.

## 4. Changes Made
- Native SwiftTerm terminal replaces WKWebView (committed `1dd8e26`, pushed origin+seal).
- Prior session-start commit this session: `7274ef2` docs handoff/make-it-state (pushed).
- App built + deployed to `~/Applications/CCDW Terminal.app` and relaunched (host-side
  artifact, not in git).

## 5. Failed Approaches -- DO NOT RETRY
- **Trying to get Terminal.app-native copy while xterm.js is in the loop** -- assumed the
  Edit-menu Copy / `window.getSelection()` could be wired to work natively in the WKWebView.
  FALSE: xterm.js sets `user-select: none` and draws its OWN selection overlay via its
  internal SelectionService, so `window.getSelection()` is always empty and WKWebView's
  native `copy:` has nothing to grab. The real selection lives only in `term.getSelection()`.
  Conclusion: a browser xterm can only ever *bridge* selection to the clipboard, never be
  truly native → this is why we went native (SwiftTerm). Do not reopen the WKWebView path.
- **Assuming ttyd exposes its xterm instance globally** -- it does not (`window.term` etc.
  absent; minified bundle, version-fragile). A JS bridge would have had to monkey-patch to
  capture the instance. Avoided entirely by dropping the web layer.
- **(carried forward) docker-compose bind mount with `bind.propagation: rslave`/`rshared` on
  macOS Docker Desktop** -- daemon rejects it (`path ... is not a shared or slave mount`),
  container won't start. Use a plain bind mount; drives added after start need
  `docker compose restart`.
- **(carried forward) GitHub headless auto-auth blocked by org SAML SSO + MFA** --
  SleepNumberInc enforces Microsoft SAML+MFA on the GitHub OAuth grant; cannot clear
  headlessly. Use a PAT / the `sn-ravance` gh account for org ops (that account has org
  access; `rob-vance_snlabs` gets "Repository not found" on push). See memory
  `github-auth-saml-sso-wall`.
- **(carried forward) Docker image build behind Zscaler fails with curl exit 60** -- SSL
  cert verify failure. Export the host Zscaler Root CA to `certs/zscaler.crt`
  (`security find-certificate -a -c Zscaler -p /Library/Keychains/System.keychain > certs/zscaler.crt`)
  before `docker compose build`.
- **(carried forward) Docker VM wedges after laptop sleep** -- `docker compose up` blocks
  forever on a dead daemon. Probe with `timeout 5 docker info`; if it hangs,
  `pkill -9 -f com.docker` (kill ALL, leave `com.docker.vmnetd`) then `open -a Docker`.

## 6. Next Steps
Goal is done. Remaining is validation + optional polish:
1. **Rob: hands-on test the copy feel** in the open `~/Applications/CCDW Terminal.app`
   window — drag-select + ⌘C, auto-copy on release, right-click Copy/Paste, ⌘V paste. Note:
   full-screen TUIs (claude/vim/less) grab the mouse; **hold Shift to force a manual text
   selection** (Terminal.app + ssh/tmux behavior). Report anything that feels off.
2. If distribution beyond this Mac is wanted: replace ad-hoc sign in
   `build-terminal-app.sh` with a Developer ID cert + notarization (unsigned/ad-hoc won't
   launch on other machines — see memory `macos-app-signing-distribution`).
3. Optional: silence the 3 cosmetic `Selector(("copy:"))` build warnings by switching the
   Edit-menu items to `#selector(NSText.copy(_:))` etc. (functionally identical).
4. Optional (only if browser terminal parity is wanted): the original plan considered
   dropping tmux from ttyd — NOT done, because going native fixed selection at the source
   and dropping tmux would only cost :7681/:7682 browser users their session persistence.

---

## Archived 2026-07-29 11:15

# Handoff -- CCDW (Claude Code Docker Workshop)
_Written 2026-07-23 11:46 by /clear-it. Read this file in full before continuing work._

## 1. Goal
Get CCDW running again. `docker compose up -d` was hanging indefinitely. Diagnose and
restore the `claude-code` container to healthy. (Ops/environment fix — no code change intended.)
Acceptance: `docker compose ps` shows `claude-code` Up + healthy, ports mapped.

## 2. Current State
- **RESOLVED.** Container `claude-code` is **Up + healthy**, ports mapped
  (3000, 3002, 7681-7682, 8080, 9200). Docker engine v29.5.3.
- Root cause: Docker Desktop's Linux VM was paused on laptop sleep (VM console log froze
  2026-07-20 16:33, "multiplexer is offline") and never cleanly resumed → engine (dockerd)
  wedged → `docker compose up` blocked forever on a dead daemon.
- Fixed by hard-killing all `com.docker*` procs + relaunching Docker Desktop clean. Engine
  came up in ~10s; `compose up -d` then succeeded.
- Branch `main`, HEAD `6ae970d`. No code changed this session.
- Pre-existing uncommitted changes untouched (prior PAT/GitHub-auth work): welcome-server.js,
  entrypoint.sh, gh-*, package*.json, .make-it-state.md, CHANGELOG.md, plus untracked
  handoff.md / .handoff-history.md / scripts/gh-token-setup.command.
- **Warning: host disk 98% full, only ~9.3Gi free** (`/System/Volumes/Data`). Docker
  builds/pulls will start failing soon. Prune recommended.

## 3. Active Files
- None edited this session. Investigation only (docker/compose ops).
- `docker-compose.yml` -- referenced during diagnosis (has the `/Volumes` bind mount from
  commit `6ae970d`); mount was NOT the cause, source `/Volumes` exists.

## 4. Changes Made
- No code or config changes. Only ran diagnostics + restarted Docker Desktop.

## 5. Failed Approaches -- DO NOT RETRY
- **First restart via `osascript quit` + `pkill -f com.docker.backend`** -- assumed quitting
  the app + killing the named backend would reboot the engine. FAILED: a stale
  `com.docker.backend` (pid 21317) survived, so `open -a Docker` saw Docker "already running"
  and never rebooted the VM; engine still down after 90s. Fix that worked: `pkill -9 -f
  "Docker Desktop"` AND `pkill -9 -f com.docker` (kill ALL docker procs; leave only the
  `com.docker.vmnetd` privileged helper), THEN `open -a Docker`.
- **Diagnosing via `docker version` / `docker info` with no timeout** -- hung indefinitely
  (that's the whole symptom — dead daemon). Always wrap docker CLI probes in `timeout 5-8`
  during an outage so the shell returns.
- **(carried forward) docker-compose bind mount with `bind.propagation: rslave`/`rshared` on
  macOS Docker Desktop** -- daemon rejects it (`path ... is not a shared or slave mount`),
  container won't start. Use plain bind mount. Drives plugged in after start need
  `docker compose restart`.
- **(carried forward) GitHub headless auto-auth blocked by org SAML SSO + MFA** -- SleepNumberInc
  enforces Microsoft SAML+MFA on the GitHub OAuth grant; cannot clear headlessly. Use PAT /
  the `sn-ravance` gh account for org ops. See memory `github-auth-saml-sso-wall`.
- **(carried forward) Docker image build behind Zscaler fails with curl exit 60** -- SSL cert
  verify failure. Export host Zscaler Root CA to `certs/zscaler.crt`
  (`security find-certificate -a -c Zscaler -p /Library/Keychains/System.keychain > certs/zscaler.crt`)
  before `docker compose build`.

## 6. Next Steps
None blocking — CCDW is up and healthy. Optional:
1. **Reclaim disk (recommended, host at 98%):** `docker system prune -a` to remove unused
   images/build cache. Omit `--volumes` unless sure — keeps `claude-code-data`,
   `claude-code-git-config`, `claude-code-gh-config`.
2. If the VM wedges again after sleep: don't wait on `compose up`; run
   `timeout 5 docker info` first — if it hangs, `pkill -9 -f com.docker` then `open -a Docker`.
3. Prior-session PAT/GitHub-auth work still uncommitted (see .handoff-history.md) — decide
   whether to finish/commit separately. This session did not touch it.

## Archived 2026-07-23 11:46

# Handoff -- CCDW (Claude Code Docker Workshop)
_Written 2026-07-15 10:35 by /clear-it. Read this file in full before continuing work._

## 1. Goal
Two independent fixes this session, both shipped:
1. **AWS Bedrock SSO sign-in** failed inside the container ("Sign-in was not completed").
   Make it complete cleanly.
2. **External drives** (macOS `/Volumes/PRO-BLADE`, etc.) + symlinks should be visible
   inside the running container.
Acceptance: AWS SSO shows a device URL + `XXXX-XXXX` code box (no localhost redirect);
`/Volumes` visible inside container.

## 2. Current State
- Branch `main`. Both fixes **committed AND pushed** to origin (`SleepNumberInc/CCDW`).
  HEAD = `6ae970d`.
- Container `claude-code` rebuilt from local image `ghcr.io/sealmindset/claude-code-docker:latest`
  and **up + healthy**. All ports mapped (3000/3002/7681-7682/8080/9200).
- Verified inside container: `/Volumes` mounted (shows `Macintosh HD`); login-wizard
  device-code fix present (grep `use-device-code` = 4 hits).
- Image **NOT pushed to registries** (GHCR/ACR) — user chose "Local only" for deploy.
  push-image.sh needs Zscaler OFF + interactive `az login`.
- Pre-existing uncommitted changes remain (welcome-server.js, entrypoint.sh, gh-*, package*.json,
  .make-it-state.md, CHANGELOG.md) — these belong to the PRIOR session's PAT/GitHub-auth work
  (see .handoff-history.md), were NOT touched this session, and were left untouched.

## 3. Active Files
- `scripts/login-wizard.sh` -- line ~700: `aws sso login ... --use-device-code` added. Fix committed.
- `docker-compose.yml` -- added bind mount host `/Volumes` → container `/Volumes`
  (overridable via `EXTERNAL_DRIVES_PATH`). Committed.
- `certs/zscaler.crt` -- exported Zscaler Root CA from macOS System keychain to unblock the
  image build (Dockerfile COPYs certs/ → update-ca-certificates). UNTRACKED, left local
  by design (repo ships certs/ empty w/ .gitkeep; each machine provides its own).

## 4. Changes Made
- login-wizard.sh: force AWS SSO device-code flow (committed `32f2262`, pushed)
- docker-compose.yml: mount /Volumes external drives (committed `6ae970d`, pushed)
- certs/zscaler.crt: exported host Zscaler CA (uncommitted, intentional — machine-specific)

## 5. Failed Approaches -- DO NOT RETRY
- **AWS SSO default (PKCE auth-code) flow inside container** -- default `aws sso login` binds
  a callback server on `127.0.0.1:PORT` INSIDE the container; the host browser redirect to
  that port can't reach it → sign-in never completes. Also the script greps for a
  `https://device.sso...` URL (expects device-code flow) → mismatch. Fix: `--use-device-code`.
  Requires AWS CLI ≥2.22; image has 2.32.7. DO NOT drop the flag.
- **docker-compose bind mount with `bind.propagation: rslave`/`rshared` on macOS Docker Desktop**
  -- daemon rejects it: `path /host_mnt/Volumes ... is not a shared or slave mount`, container
  won't start. macOS host mount isn't marked shared. Fix: plain bind mount (no propagation).
  Consequence: drives plugged in AFTER container start need `docker compose restart`. Native
  Linux hosts CAN add `propagation: rslave` for live propagation.
- **(carried forward) GitHub headless auto-auth blocked by org SAML SSO + MFA** -- SleepNumberInc
  enforces Microsoft SAML+MFA on the GitHub OAuth grant; cannot clear headlessly. This session
  it also blocked `git push`: active gh account `rob-vance_snlabs` got "Repository not found";
  had to `gh auth switch --user sn-ravance` (the account with org access) before push worked.
  Use PAT / the sn-ravance account for org ops. See memory `github-auth-saml-sso-wall`.
- **(carried forward) Docker image build behind Zscaler fails with curl exit 60** -- SSL cert
  verify failure on github.com download. certs/ was empty. Fix: export host Zscaler Root CA
  (`security find-certificate -a -c Zscaler -p /Library/Keychains/System.keychain > certs/zscaler.crt`)
  before `docker compose build`.

## 6. Next Steps
None blocking — both fixes done, committed, pushed, verified in the running container.
Optional / when needed:
1. To test AWS SSO end-to-end: run the login wizard in the container, confirm it shows the
   device URL + `XXXX-XXXX` code box (not a `127.0.0.1` redirect), complete sign-in.
2. To test external drive: plug in PRO-BLADE, `docker compose restart claude`,
   then `docker exec claude-code ls /Volumes/` — confirm it appears.
3. If distributing the image to other machines: run `scripts/push-image.sh` with Zscaler
   OFF + `az login` to push GHCR + ACR (skipped this session — local-only deploy).
4. Prior-session PAT/GitHub-auth work is still uncommitted (see .handoff-history.md) — decide
   whether to finish/commit it separately.

## Archived 2026-07-15 10:35

# Handoff -- CCDW (Claude Code Docker Workshop)
_Written 2026-07-10 by /clear-it. Read this file in full before continuing work._

## 1. Goal
Make CCDW's dashboard "GitHub" status card give business users **zero-touch GitHub sign-in**:
click card → authenticated, no manual steps. Blocker discovered: org enforces SAML SSO + MFA
via Microsoft on the GitHub OAuth grant, so headless device-flow auth cannot be fully
automated. Decision (user-chosen): pivot to a **Personal Access Token (PAT)** stored once,
loaded automatically. Acceptance: click card (or container start) → `gh auth status` logged
in, no browser.

## 2. Current State
- Branch `main`. Uncommitted changes present (see Changes Made). NOTHING committed this session.
- Container `claude-code` up + healthy (~hours). All services 200 (dashboard 3000, workshop
  9200, chat 3002, terminal 7681, vscode 8080→302). Deploy earlier this session pushed fresh
  image to GHCR + ACR (digest e6a7a3ab...).
- GitHub in the running container **is currently logged in** as `sn-ravance` (gho_ token in
  mounted `~/.config/gh` volume, persists across restarts) — from a manual device-flow
  completion. Dashboard card shows green (`github: "ok"`).
- PAT zero-touch wiring is CODE-COMPLETE but NOT yet tested end-to-end (waiting on user to
  create + save a token). Running container still has OLD welcome-server.js (edits not
  hot-loaded yet); entrypoint change only takes effect on next container start.

## 3. Active Files
- `scripts/welcome-server.js` -- `/auth/github-auto` now tries stored PAT first (gh auth login
  --with-token from GH_TOKEN/GITHUB_TOKEN env or `/home/coder/Documents/.ccdw/gh-token`), falls
  back to device flow. NOT hot-loaded into running container.
- `scripts/entrypoint.sh` -- new startup block auto-signs-in gh from token file/env (su-exec
  coder). Only runs at container start.
- `scripts/gh-token-setup.command` -- NEW host helper; prompts for PAT (hidden), writes
  `~/Documents/.ccdw/gh-token` chmod 600.
- `scripts/gh-playwright-auth.mjs` -- rewrote grant flow as state machine (Verify Session →
  code split-boxes → Authorize). Now exits code 4 = sso_required on Microsoft/SAML redirect.
- `scripts/gh-session-setup.command` + `.mjs` -- seeder fixes (see Failed Approaches).
- `package.json` / `package-lock.json` -- playwright@1.61.1 added to CCDW root so the host
  seeder's `import 'playwright'` resolves. Decide whether to keep or move to a scoped install.

## 4. Changes Made (all UNCOMMITTED)
- welcome-server.js: PAT-first path in /auth/github-auto
- entrypoint.sh: token auto-login block at startup
- gh-token-setup.command: new PAT seeder (host)
- gh-playwright-auth.mjs: multi-screen state machine + sso_required exit 4
- gh-session-setup.command: install playwright into repo (ESM fix), run `node` directly
- gh-session-setup.mjs: cookie-based login detection (user_session/dotcom_user), no navigation
- package.json/lock: added playwright dep
- gh-playwright-auth.mjs was hot-copied into the running container for testing (container-only,
  not persisted to image).

## 5. Failed Approaches -- DO NOT RETRY
- **Seeder via `npx -y -p playwright node script.mjs`** -- assumed npx `-p` makes the module
  importable. Failed: ESM resolves bare `import 'playwright'` from the SCRIPT's dir tree, not
  npx's temp node_modules → ERR_MODULE_NOT_FOUND. Fix: install playwright into the repo so it
  resolves up-tree (done in .command). For ad-hoc probes, import by ABSOLUTE path
  `/opt/claude-code-docker/node_modules/playwright/index.js` and read `.chromium` off the
  namespace (destructuring `{chromium}` from the abs path returned undefined).
- **Seeder detecting login by navigating to /settings/profile every 2s** -- yanked the tab away
  mid-login so the user could never finish; always timed out. Fix: watch cookies, never navigate.
- **Cookie check requiring `logged_in=yes` + `user_session`** -- too strict, missed real login.
  Fix: match ANY of user_session / dotcom_user / __Host-user_session_same_site on *.github.com.
- **`page.goto(github, {waitUntil:'networkidle'})`** -- never settles on GitHub (long-poll),
  30s timeout. Use `domcontentloaded`.
- **Headless GitHub device-flow auto-authorize with a seeded GitHub session** -- assumed the
  GitHub cookie seed was enough. FALSE: after clicking Authorize, org policy redirects to
  `login.microsoftonline.com/.../saml2` (MFA). Cannot clear MFA headlessly, and it's a headless
  container (no window for the user). This is why the user must use a PAT. Playwright drives
  GitHub's own screens fine; the wall is Microsoft SSO+MFA. See memory
  `github-auth-saml-sso-wall`.
- **Treating an Authorize-button CLICK as success** -- gave a false "authorized" while gh never
  got a token (SAML intercepted). Only success TEXT (congratulations/connected) counts.

## 6. Next Steps
1. **User action (blocking):** create a GitHub PAT (classic: scopes repo, read:org, gist),
   click Configure SSO → Authorize for org SleepNumberInc, then run
   `~/Documents/CCDW/scripts/gh-token-setup.command` and paste it (writes ~/Documents/.ccdw/gh-token).
2. Hot-load new welcome-server.js into the running container and restart the welcome-server
   process as coder (WELCOME_DIR/WELCOME_PORT node welcome-server.js). Then POST /auth/github-auto
   and confirm `{needed:false, method:"token"}` with NO browser; verify `gh auth status` green.
3. Decide package.json disposition (keep playwright dep at repo root, or make the seeder use a
   scoped/temp install so CCDW's root deps stay clean).
4. Commit all changes (both remotes: origin + seal) and redeploy image (scripts/push-image.sh,
   GHCR+ACR) to bake in entrypoint token auto-login + PAT path + seeder fixes.
5. `/make-it` was just updated v1.18.0 → v1.19.0 — a Claude Code restart is pending for that to
   take effect (unrelated to CCDW code).
