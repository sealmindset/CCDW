# Project State -- Claude Code Docker
> Last updated: 2026-04-29
> Last session: macos-setup-script
> Session ended: 2026-04-29

## Current Status
Fully operational containerized Claude Code environment with Workshop (Business User IDE),
self-healing infrastructure, web terminal, and VS Code in browser.
Multi-provider install support complete: `--ai=foundry|bedrock|anthropic` with per-provider
JSON configs, data-driven preflight checks, and interactive prompts for missing values.
Both Windows (setup-claude.bat) and macOS (setup-claude-mac.command) one-click bootstrap
scripts complete — auto-install Rancher Desktop, pre-configure engine, connectivity checks,
Gatekeeper handling, and handoff to install.command/install.bat.

## This Session
- Created setup-claude-mac.command: one-click macOS bootstrap script for CTO laptop
  - Internet + VPN connectivity pre-checks with friendly recovery prompts
  - 3-tier Rancher Desktop auto-install (Homebrew → direct DMG download → browser fallback)
  - Pre-configures Rancher Desktop settings.json to use dockerd/moby (skips first-run dialog)
  - Gatekeeper quarantine removal on self and install.command
  - Auto-launches Rancher Desktop with 2-minute Docker daemon polling loop
  - Architecture-aware DMG download (arm64/x86_64)
  - Detects if script is inside repo vs needs to clone CCDW
  - Hands off to install.command --ai=foundry via exec
  - Elapsed timer shows setup progress
- Committed and verified syntax clean (bash -n)

## Outstanding Items
- Push setup-claude-mac.command to remotes (sealmindset and/or sleepnumberinc)
- Test setup-claude-mac.command on CTO MacBook Pro (10-minute target)
- Test install.bat --ai=foundry on Windows
- Test install.bat --ai=bedrock on Windows
- Test install.command --ai=bedrock on macOS
- Test Bedrock aws sso login inside container with real AWS account
- RBAC model decision (Object_ID-based vs Application-based)
- /ship-it and /argo-it deployment automation

## Test Status
- setup-claude-mac.command: bash -n syntax check passing
- All config JSON files: valid JSON
- Shell scripts: bash -n syntax check passing (configure-provider.sh, login-wizard.sh, shell-init.sh, install.command)
- install.bat: basic paren balance check passing
- Playwright tests: 37/37 passing (Workshop views + Bifrost E2E)
- Last run: 2026-04-29

## Known Issues
- None currently active

## Suggested Next Steps
1. Push to remotes and test setup-claude-mac.command on CTO MacBook Pro
2. Test install.bat --ai=foundry on Windows (real hardware)
3. RBAC model decision for Entra ID integration
