# Project State -- Claude Code Docker
> Last updated: 2026-05-13
> Last session: CEO UX polish + pre-install access tools
> Session ended: 2026-05-13

## Current Status
Fully operational containerized Claude Code environment with Workshop (Business User IDE),
self-healing infrastructure, web terminal, and VS Code in browser.
Multi-provider install support complete: `--ai=foundry|bedrock|anthropic` with per-provider
JSON configs, data-driven preflight checks, and interactive prompts for missing values.
Ship view overhauled with real readiness checks and three deployment modes.
All user-facing text polished for non-technical/executive audience.

## This Session
- Wired /ship-it and /argo-it into Workshop with real readiness checks
  - Real server-side readiness API (POST /api/ship/readiness)
  - Three modes: Ship for Review, Save Progress, Deploy to Kubernetes
  - kubectl + Helm added to Docker image
  - Live progress feed during deployment
- CEO/executive UX polish across all touchpoints:
  - Welcome dashboard: removed port badges, renamed jargon labels, simplified descriptions
  - Shell init: friendly PS1 prompt, "Claude Code" branding throughout
  - Login wizard: plain language preflight labels, generic step headers
  - macOS install: preamble, progress dots, silent SSL, single URL ending
  - Windows install: same polish — preamble, simpler errors, single URL ending
- Added pre-install access checker scripts (check-access.command, check-access.bat)
  - Verifies Docker, internet, SSL, disk, provider access without installing
  - Reports pass/fail with "who to contact" for each failure
- Added printable HTML checklist (docs/before-you-install.html)
  - Interactive checkboxes with localStorage persistence
  - Dark theme matching welcome dashboard, print-friendly light mode
  - Platform badges, "who to contact" hints, progress bar

## Outstanding Items
- Test install.bat --ai=foundry on Windows
- Test install.bat --ai=bedrock on Windows
- Test install.command --ai=bedrock on macOS
- Test Bedrock aws sso login inside container with real AWS account
- Test install.bat on Windows with Rancher Desktop (under SSMITH admin)
- Test install.bat on Windows with Docker Desktop
- Verify Docker socket mounting on Windows hosts
- RBAC model decision (Object_ID-based vs Application-based)
- E2E test: build app with /make-it, then /ship-it creates PR on GitHub
- E2E test: build app with /make-it, then /argo-it generates K8s manifests + PR
- Add kubeconfig mounting from host for /argo-it cluster access

## Test Status
- Shell scripts: syntax checks passing
- Config JSON files: valid JSON
- Playwright tests: 37/37 passing (Workshop views + Bifrost E2E)
- Last run: 2026-05-12

## Known Issues
- sealmindset remote reports "This repository moved" — should update to SleepNumberInc/CCDW.git

## Suggested Next Steps
1. Test install + check-access on real Windows hardware with Linda
2. Test Bedrock end-to-end (install + SSO login inside container)
3. RBAC model decision for Entra ID integration
