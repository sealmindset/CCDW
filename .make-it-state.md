# Project State -- Claude Code Docker
> Last updated: 2026-05-12
> Last session: setup-hardening
> Session ended: 2026-05-12

## Current Status
Fully operational containerized Claude Code environment with Workshop (Business User IDE),
self-healing infrastructure, web terminal, and VS Code in browser.
Multi-provider install support complete: `--ai=foundry|bedrock|anthropic` with per-provider
JSON configs, data-driven preflight checks, and interactive prompts for missing values.
Both Windows (setup-claude.bat) and macOS (setup-claude-mac.command) one-click bootstrap
scripts complete — auto-install Rancher Desktop, pre-configure engine, connectivity checks,
Gatekeeper handling, and handoff to install.command/install.bat.

## This Session
- Mounted Documents, Downloads, and Desktop into container for file access
- Applied Mac setup fixes to Windows bat files (parity)
- Added Claude starburst icon to desktop shortcut
- Hardened macOS setup for non-technical users
- Fixed network check failing on corporate network
- Fixed macOS setup for long usernames and Finder PATH
- Added Lima socket path fix for long macOS usernames
- Added sudo, persistent git identity, multi-terminal, app dashboard, login UX
- Added GitHub auth flow to setup-claude.bat and auto-set PROJECTS_PATH

## Outstanding Items
- Test install.bat --ai=foundry on Windows
- Test install.bat --ai=bedrock on Windows
- Test install.command --ai=bedrock on macOS
- Test Bedrock aws sso login inside container with real AWS account
- Test install.bat on Windows with Rancher Desktop (under SSMITH admin)
- Test install.bat on Windows with Docker Desktop
- Verify Docker socket mounting on Windows hosts
- RBAC model decision (Object_ID-based vs Application-based)
- /ship-it and /argo-it deployment automation

## Test Status
- Shell scripts: syntax checks passing
- Config JSON files: valid JSON
- Playwright tests: 37/37 passing (Workshop views + Bifrost E2E)
- Last run: 2026-05-12

## Known Issues
- None currently active

## Suggested Next Steps
1. Test install.bat --ai=foundry on real Windows hardware
2. Test Bedrock end-to-end (install + SSO login inside container)
3. RBAC model decision for Entra ID integration
