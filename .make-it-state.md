# Project State -- CCDW (Claude Code Docker Workshop)
> Last updated: 2026-07-06
> Last session: wrap-it
> Session ended: 2026-07-06

## Current Status
Fully working containerized Claude Code environment with 4 AI providers (Foundry, Bedrock, Anthropic, Claude Account). All services operational: Web Terminal (7681), VS Code (8080), Welcome Dashboard (3000), Workshop IDE (9200), Claude Chat (3002). Shared top navigation bar now links all pages.

## This Session
- Added shared top navigation bar across Dashboard, Workshop, and Claude Chat
  - Sticky 44px bar with breadcrumb-style links to all 5 services
  - Active page highlighted by port detection, responsive (labels hide on mobile)
  - Files: shared-nav.css + shared-nav.js (copied to each service's public dir)

## Outstanding Items
- Test `--ai=claude` on macOS and Windows
- Test `--ai=foundry` and `--ai=bedrock` on Windows
- End-to-end /ship-it and /argo-it deployment tests
- Docker socket on macOS (Colima, Docker Desktop, Rancher Desktop)
- RBAC model decision (Object_ID vs Application-based)
- Test install.bat on Windows with Rancher Desktop (under SSMITH admin)
- Test install.bat on Windows with Docker Desktop

## Test Status
- Unit tests: not yet set up
- Integration tests: not yet set up
- E2E tests: Playwright infra exists (37 tests) but not wired to CI
- Last run: manual curl tests for Chat + Bedrock (2026-05-26, passing)

## Known Issues
- Workshop ideation chat uses Opus by design (follows /make-it references) -- slower than Sonnet but intentional
- `self-heal-lib.sh` logs `ANTHROPIC_API_KEY: unbound variable` when Bedrock provider active (cosmetic)

## Suggested Next Steps
1. Test shared nav bar in-browser (rebuild container, visit each port, confirm nav works)
2. Test `--ai=claude` on macOS (`install.command --ai=claude`) and verify OAuth flow
3. Test Windows installers (`install.bat --ai=bedrock`, `--ai=claude`)
