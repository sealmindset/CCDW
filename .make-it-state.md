# Project State -- Claude Code Docker
> Last updated: 2026-04-21
> Last session: provider-install-refactor
> Session ended: 2026-04-21

## Current Status
Fully operational containerized Claude Code environment with Workshop (Business User IDE),
self-healing infrastructure, web terminal, and VS Code in browser.
Install scripts refactored: accept `--ai=foundry|bedrock|anthropic` argument,
load defaults from per-provider JSON config files in `config/`.
AWS CLI v2 now installed in Docker image for Bedrock SSO support.
Login wizard is provider-aware (Azure device-code + AWS SSO flows).

## This Session
- Created per-provider config JSON files (6 files):
  - config/foundry.json -- Sleep Number defaults (endpoint, models, subscription)
  - config/foundry.template.json -- blank template with help text
  - config/bedrock.json -- AWS defaults (region, models, SSO profile)
  - config/bedrock.template.json -- blank template with help text
  - config/anthropic.json -- minimal (just needs API key)
  - config/anthropic.template.json -- blank template with help text
- Refactored install.command (macOS):
  - Accepts --ai=foundry|bedrock|anthropic argument
  - Interactive provider menu when no argument given
  - Loads defaults from config JSON, prompts only for missing values
  - Writes .env from config JSON (comments out old provider keys)
  - Writes ~/.aws/config for Bedrock SSO
  - Mounts ~/.aws volume for Bedrock
- Refactored install.bat (Windows):
  - Same --ai= argument support
  - Node.js path: reads config JSON and writes .env programmatically
  - CLI fallback: reads defaults from config JSON via PowerShell
  - Bedrock CLI fallback writes ~/.aws/config via PowerShell
- Added AWS CLI v2 to Dockerfile (apk add aws-cli)
- Refactored login-wizard.sh to be provider-aware:
  - Detects active provider from env vars
  - Azure Foundry: same device-code flow (preflight, sign-in, allset)
  - AWS Bedrock: new SSO flow (preflight checks AWS CLI v2 + SSO profile, aws sso login, allset with STS verification)
  - Anthropic: no login needed, exits cleanly
- Updated shell-init.sh:
  - AUTH_OK replaces AZ_OK for provider-agnostic auth checking
  - Bedrock: checks aws sts get-caller-identity for SSO session validity
  - Returning user: triggers login-wizard --refresh for expired Bedrock SSO
  - Status display shows AWS SSO state for Bedrock users

## Outstanding Items
- Test install.bat end-to-end with --ai=foundry on Windows
- Test install.bat end-to-end with --ai=bedrock on Windows
- Test install.command end-to-end on macOS
- Test Bedrock aws sso login flow inside container
- RBAC model decision (Object_ID-based vs Application-based)
- /ship-it and /argo-it deployment automation

## Test Status
- All config JSON files: valid JSON (node parse check)
- Shell scripts: bash -n syntax check passing
- Docker build: successful with AWS CLI v2
- Container: healthy, AWS CLI v2.32.7 + Azure CLI 2.85.0 both available
- Workshop API: responding correctly (read-only provider status)
- Playwright tests: 37/37 passing (Workshop views + Bifrost E2E)

## Known Issues
- None currently active

## Suggested Next Steps
1. Test install.command --ai=bedrock on macOS end-to-end
2. Test Bedrock SSO login flow inside container with real AWS account
3. Test install.bat --ai=foundry on Windows
