# Project State -- CCDW (Claude Code Docker Workshop)
> Last updated: 2026-05-26
> Last session: wrap-it
> Session ended: 2026-05-26T19:05Z

## Current Status
Fully working containerized Claude Code environment with 4 AI providers (Foundry, Bedrock, Anthropic, Claude Account). All services operational: Web Terminal (7681), VS Code (8080), Welcome Dashboard (3000), Workshop IDE (9200), Claude Chat (3002).

## This Session
- Added `--ai=claude` provider across 19 files (OAuth login, no API key needed)
- Fixed Workshop banner showing "not configured" for Bedrock/Claude providers
- Built full Bedrock support in Chat: SigV4 signing, binary event stream parser, model list
- Fixed SigV4 signature mismatch for model IDs with colons (e.g., `v1:0`)
- Fixed Alpine AWS CLI `--format json` flag incompatibility
- Pushed multi-platform image (amd64+arm64) to GHCR and ACR
- Verified Chat + Bedrock Opus end-to-end (2.5s API response time)
- Verified CLI + Bedrock Opus inside container (4.5s total, 2.5s API)

## Outstanding Items
- Test `--ai=claude` on macOS and Windows
- Test `--ai=foundry` and `--ai=bedrock` on Windows
- End-to-end /ship-it and /argo-it deployment tests
- Docker socket on macOS (Colima, Docker Desktop, Rancher Desktop)
- RBAC model decision (Object_ID vs Application-based)
- Test install.bat on Windows with Rancher Desktop (under SSMITH admin)
- Test install.bat on Windows with Docker Desktop

## Test Status
- Unit tests: not yet set up
- Integration tests: not yet set up
- E2E tests: Playwright infra exists (37 tests) but not wired to CI
- Last run: manual curl tests for Chat + Bedrock (2026-05-26, passing)

## Known Issues
- Workshop ideation chat uses Opus by design (follows /make-it references) -- slower than Sonnet but intentional
- `self-heal-lib.sh` logs `ANTHROPIC_API_KEY: unbound variable` when Bedrock provider active (cosmetic)

## Suggested Next Steps
1. Test `--ai=claude` on macOS (`install.command --ai=claude`) and verify OAuth flow
2. Test Windows installers (`install.bat --ai=bedrock`, `--ai=claude`)
3. Set up CI test suite (pytest + Playwright for Workshop browser tests)
