# Project State -- Claude Code Docker
> Last updated: 2026-04-22
> Last session: multi-provider-docs
> Session ended: 2026-04-22

## Current Status
Fully operational containerized Claude Code environment with Workshop (Business User IDE),
self-healing infrastructure, web terminal, and VS Code in browser.
Multi-provider install support complete: `--ai=foundry|bedrock|anthropic` with per-provider
JSON configs, data-driven preflight checks, and interactive prompts for missing values.
AWS CLI v2 in Docker image. Login wizard handles Azure device-code, AWS SSO, and API key.
README updated with new install docs. CCDW Setup Guide (Chapter 11) published to MakeIt-TheDocs.

## This Session
- Added interactive prompts in install.bat for missing provider values (Foundry endpoint,
  API key, Bedrock SSO details, Anthropic API key) using PowerShell -- matching install.command
- Rebuilt Docker image with all updated scripts (configure-provider.sh, login-wizard.sh, shell-init.sh)
- Verified Bedrock settings.json generation in container (awsAuthRefresh, permissions, all model vars)
- Verified AWS CLI v2.32.7 present in container
- Updated README.md: added installer-driven setup (Option A) alongside manual .env (Option B),
  updated project structure with config/ files and current scripts
- Created Chapter 11: CCDW Setup Guide in MakeIt-TheDocs (500 lines)
  - Architecture overview, data flow, config-driven design
  - All three provider paths with Sleep Number specifics
  - Installation walkthrough (macOS + Windows)
  - Docker image internals, ports, volumes
  - Troubleshooting table, org customization guide, quick reference
- Pushed all changes to sealmindset + sleepnumberinc remotes

## Outstanding Items
- Test install.bat --ai=foundry on Windows
- Test install.bat --ai=bedrock on Windows
- Test install.command --ai=bedrock on macOS
- Test Bedrock aws sso login inside container with real AWS account
- Clean up old workshop setup files (setup.html no longer primary)
- RBAC model decision (Object_ID-based vs Application-based)
- /ship-it and /argo-it deployment automation

## Test Status
- All config JSON files: valid JSON
- Shell scripts: bash -n syntax check passing (configure-provider.sh, login-wizard.sh, shell-init.sh, install.command)
- install.bat: basic paren balance check passing
- Docker build: successful with AWS CLI v2
- Container: healthy, AWS CLI v2.32.7 + Azure CLI available
- Bedrock settings.json: verified correct output in container
- Playwright tests: 37/37 passing (Workshop views + Bifrost E2E)

## Known Issues
- None currently active

## Suggested Next Steps
1. Test install.command --ai=bedrock on macOS with real AWS account
2. Test install.bat --ai=foundry and --ai=bedrock on Windows
3. Clean up old workshop setup files (setup.html, setup.js, setup.css, setup/server.js)
