{
  "provider": "foundry",
  "display_name": "Azure AI Foundry",
  "endpoint": "https://snapistg-scus.azure.sleepnumber.com/anthropic",
  "auth_mode": "sso",
  "api_key": "",
  "token_resource": "https://cognitiveservices.azure.com",
  "subscription_id": "a8da1709-ccc3-4356-b3b7-6660af86e979",
  "subscription_name": "sn-openai-dev-01",
  "models": {
    "sonnet": "cogdep-aifoundry-dev-eus2-claude-sonnet-4-6",
    "haiku": "cogdep-aifoundry-dev-eus2-claude-haiku-4-5",
    "opus": "cogdep-aifoundry-dev-eus2-claude-opus-5"
  },
  "default_model": "opus",
  "env_vars": {
    "CLAUDE_CODE_USE_FOUNDRY": "1",
    "ANTHROPIC_FOUNDRY_BASE_URL": "{endpoint}",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "{models.sonnet}",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "{models.haiku}",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "{models.opus}"
  },
  "env_vars_optional": {
    "ANTHROPIC_FOUNDRY_API_KEY": "{api_key}"
  },
  "settings_json": {
    "apiKeyHelper": "/home/coder/.claude/get-claude-token.sh",
    "env": {
      "CLAUDE_CODE_USE_FOUNDRY": "1",
      "ANTHROPIC_FOUNDRY_BASE_URL": "{endpoint}",
      "ANTHROPIC_DEFAULT_SONNET_MODEL": "{models.sonnet}",
      "ANTHROPIC_DEFAULT_HAIKU_MODEL": "{models.haiku}",
      "ANTHROPIC_DEFAULT_OPUS_MODEL": "{models.opus}"
    }
  },
  "prereqs": {
    "host": [
      {
        "id": "network",
        "label": "AI Foundry endpoint reachable",
        "check": "curl -so /dev/null -w '%{http_code}' --connect-timeout 10 https://snapistg-scus.azure.sleepnumber.com | grep -qv '^000$'",
        "required": true,
        "fail_message": "Cannot reach the AI Foundry endpoint. You need corporate network (office Wi-Fi) or VPN (GlobalProtect). If on corporate network, Zscaler SSL inspection may be blocking the request.",
        "retry": true
      },
      {
        "id": "azure-subscription",
        "label": "Azure sign-in happens automatically after install (no action needed now)",
        "check": "info",
        "required": false,
        "fail_message": ""
      },
      {
        "id": "internet",
        "label": "Internet connectivity",
        "check": "curl -sf --connect-timeout 5 https://login.microsoftonline.com",
        "required": true,
        "fail_message": "No internet connection. Check your network."
      },
      {
        "id": "zscaler-note",
        "label": "SSL inspection (Zscaler) handled automatically",
        "check": "info",
        "required": false,
        "fail_message": "The installer auto-extracts corporate certificates. If Docker pulls still fail, contact IT about your content filter settings."
      }
    ],
    "container": [
      {
        "id": "azure-cli",
        "label": "Azure CLI",
        "check": "az --version",
        "expect": "azure-cli"
      },
      {
        "id": "azure-login",
        "label": "Azure AD signed in",
        "check": "az account show",
        "fail_message": "Not signed in to Azure. Run: az login --use-device-code"
      },
      {
        "id": "azure-subscription",
        "label": "Access to Azure subscription: sn-openai-dev-01",
        "check": "az account list --query \"[?id=='a8da1709-ccc3-4356-b3b7-6660af86e979'].id\" -o tsv | grep -q a8da1709",
        "fail_message": "Your account does not have access to the required Azure subscription (sn-openai-dev-01). Ask your manager to request access."
      },
      {
        "id": "azure-token",
        "label": "Azure token for Cognitive Services",
        "check": "az account get-access-token --resource https://cognitiveservices.azure.com --query accessToken -o tsv",
        "fail_message": "Cannot get Azure token. Run: login (to re-authenticate)"
      },
      {
        "id": "endpoint-reachable",
        "label": "AI Foundry endpoint reachable",
        "check": "curl -sf --connect-timeout 8 https://snapistg-scus.azure.sleepnumber.com/anthropic",
        "fail_message": "Cannot reach AI Foundry. Make sure VPN is connected on the host machine."
      }
    ]
  },
  "docker_requires": ["azure-cli"],
  "container_login": "azure-device-code"
}
