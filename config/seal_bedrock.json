{
  "provider": "bedrock",
  "display_name": "AWS Bedrock",
  "region": "us-east-1",
  "sso_start_url": "",
  "sso_region": "us-east-1",
  "account_id": "",
  "role_name": "",
  "profile_name": "sso-bedrock",
  "models": {
    "default": "sonnet",
    "sonnet": "us.anthropic.claude-sonnet-4-6",
    "haiku": "us.anthropic.claude-haiku-4-5-20251001-v1:0",
    "opus": "us.anthropic.claude-opus-4-6-v1"
  },
  "default_model": "sonnet",
  "env_vars": {
    "CLAUDE_CODE_USE_BEDROCK": "1",
    "AWS_REGION": "{region}",
    "AWS_PROFILE": "{profile_name}",
    "ANTHROPIC_MODEL": "{models.default}",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "{models.sonnet}",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "{models.haiku}",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "{models.opus}",
    "DISABLE_PROMPT_CACHING": "0"
  },
  "aws_config": {
    "sso_session": "aws-sso",
    "sso_start_url": "{sso_start_url}",
    "sso_region": "{sso_region}",
    "sso_registration_scopes": "sso:account:access",
    "sso_account_id": "{account_id}",
    "sso_role_name": "{role_name}"
  },
  "settings_json": {
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "awsAuthRefresh": "aws sso login --profile {profile_name}",
    "env": {
      "AWS_PROFILE": "{profile_name}",
      "CLAUDE_CODE_USE_BEDROCK": "1",
      "ANTHROPIC_MODEL": "{models.default}",
      "ANTHROPIC_DEFAULT_HAIKU_MODEL": "{models.haiku}",
      "ANTHROPIC_DEFAULT_SONNET_MODEL": "{models.sonnet}",
      "ANTHROPIC_DEFAULT_OPUS_MODEL": "{models.opus}",
      "DISABLE_PROMPT_CACHING": "0"
    },
    "permissions": {
      "deny": [
        "Read(~/.aws/**)",
        "Read(./.env)",
        "Read(./.env.*)",
        "Read(./secrets/**)"
      ],
      "ask": ["WebFetch", "Bash(curl:*)"]
    }
  },
  "prereqs": {
    "host": [
      {
        "id": "aws-cli-v2",
        "label": "AWS CLI v2",
        "check": "aws --version",
        "expect": "aws-cli/2",
        "required": true,
        "fail_message": "AWS CLI v2 is required. Install from: https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html"
      },
      {
        "id": "internet",
        "label": "Internet connectivity",
        "check": "curl -sf --connect-timeout 5 https://aws.amazon.com",
        "required": true,
        "fail_message": "No internet connection. Check your network."
      }
    ],
    "container": [
      {
        "id": "aws-cli-v2",
        "label": "AWS CLI v2",
        "check": "aws --version",
        "expect": "aws-cli/2"
      },
      {
        "id": "aws-sso-profile",
        "label": "AWS SSO profile configured",
        "check": "grep -q 'profile {profile_name}' ~/.aws/config",
        "fail_message": "AWS SSO profile not found in ~/.aws/config. Re-run the installer with --ai=bedrock."
      },
      {
        "id": "aws-sso-session",
        "label": "AWS SSO session active",
        "check": "aws sts get-caller-identity --profile {profile_name}",
        "fail_message": "AWS SSO session expired. Run: aws sso login --profile {profile_name}"
      }
    ]
  },
  "docker_requires": ["aws-cli"],
  "container_login": "aws-sso",
  "_help": {
    "region": "AWS region where Bedrock models are available (e.g. us-east-1, us-west-2)",
    "sso_start_url": "Your AWS IAM Identity Center start URL (e.g. https://d-xxxxxxxxxx.awsapps.com/start)",
    "sso_region": "Region where your IAM Identity Center is configured",
    "account_id": "AWS account ID with Bedrock model access (12-digit number)",
    "role_name": "IAM Identity Center role name with Bedrock permissions",
    "profile_name": "Local AWS profile name (used in ~/.aws/config and settings.json)",
    "models": "Bedrock model IDs -- defaults are standard Claude model IDs for Bedrock"
  }
}
